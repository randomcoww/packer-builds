{
  "variables": {
    "user": "randomcoww",
    "uid": "10000",
    "password": "password",
    "hostname": "docker6",
    "chef_environment": "testing"
  },

  "builders": [
    {
      "name": "{{user `hostname`}}",

      "type": "qemu",
      "format": "qcow2",
      "accelerator": "kvm",

      "iso_url": "http://releases.ubuntu.com/14.04/ubuntu-14.04.2-server-amd64.iso",
      "iso_checksum": "83aabd8dcf1e8f469f3c72fff2375195",
      "iso_checksum_type": "md5",

      "http_directory": "http",

      "ssh_username": "root",
      "ssh_key_path": "files/packer.key",
      "shutdown_command": "shutdown -P now",

      "boot_wait": "2s",
      "qemuargs": [
        [ "-display", "none" ]
      ],

      "boot_command": [
        "<esc><esc><enter><wait>",
        "/install/vmlinuz ks=http://{{ .HTTPIP }}:{{ .HTTPPort }}/ks-1404minimalvm.cfg ",
        "hostname={{user `hostname`}} ",
          
        "passwd/user-fullname={{user `user`}} ",
        "passwd/user-password-again={{user `password`}} ",
        "passwd/user-password={{user `password`}} ",
        "passwd/user-uid={{user `uid`}} ",
        "passwd/username={{user `user`}} ",

        "initrd=/install/initrd.gz -- <enter>"
      ]
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "inline": [
        "mkdir /etc/chef",
        "apt-get -qq -y autoremove",
        "apt-get clean",
        "rm -f /var/cache/apt/*cache.bin",
        "rm -f /var/lib/apt/lists/*",
        "apt-get -y remove nano",
        "apt-get -y update",
        "apt-get -y upgrade",
        "apt-get -y install curl vim wget git cgroup-bin nfs-client"
      ]
    },
    {
      "type": "file",
      "source": "/etc/chef/encrypted_data_bag_secret",
      "destination": "/etc/chef/encrypted_data_bag_secret"
    },
    {
      "type": "chef-client",
      "server_url": "https://api.opscode.com/organizations/madcoww",
      "chef_environment": "testing",
      "prevent_sudo": true,
      "node_name": "{{user `hostname`}}",
      "skip_clean_node": true,
      "skip_clean_client": true,
      "config_template": "files/config_template",
      "validation_client_name": "madcoww-validator",
      "validation_key_path": "/etc/chef/madcoww-validator.pem",
      "json": {
        "chef_client": {
          "daemon_options": ["-E", "{{user `chef_environment`}}"],
          "init_style": "runit",
          "config": {
            "node_name": "{{user `hostname`}}"
          }
        },
        "docker": {
          "version": "1.6.2",
          "storage_driver": "devicemapper"
        },
        "set_fqdn": "*.static.local"
      },
      "run_list": [
        "recipe[hostname]",
        "recipe[runit]",
        "recipe[chef-client]",
        "recipe[chef-client::config]",
        "recipe[docker]"
      ]
    },
    {
      "type": "shell",
      "inline": ["rm /root/.ssh/authorized_keys"]
    }
  ]
}
